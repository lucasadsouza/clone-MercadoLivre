import {  StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    btn_primary: {
        width: '100%',
        backgroundColor: '#3483f4',
        borderRadius: 3,
        height: 60,
        justifyContent: 'center',
        alignItems: 'center',
        marginTop: 10,
    },

    btn_outline_primary: {
        width: '100%',
        backgroundColor: '#fff',
        borderRadius: 3,
        borderColor: '#3483f4',
        borderWidth: 1,
        height: 60,
        justifyContent: 'center',
        alignItems: 'center',
        marginTop: 10,
    }
});

export default styles;